//@version=5
indicator(title='DRE Seasonality-Buschi year-week-day', shorttitle='DRE Seasonality-Buschi ywd', precision=2, max_bars_back=5000)

iNumberOfPeriods = input.int(12, "Number of periods", maxval = 24)
iYear = input.bool(true, "year", group = "GRP1")
iMnth = input.bool(false, "month", group = "GRP1")
iWeek = input.bool(false, "week", group = "GRP1")
iDay = input.bool(false, "day", group = "GRP1")
iOnlyLastPeriod = input.bool(true, "Draw only last period")

repeat_period_millis = 1 * 24 * 60 * 60 * 1000

fast_search_n(xs, x) =>  // xs - sorted, ascending
    len = 4096
    size = math.floor(len / 2)
    k = size
    for i = 0 to 15 by 1
        size := math.floor(size / 2)
        if (x <= xs[k] ? 1 : 0) * (x > xs[k + 1] ? 1 : 0) > 0
            break
        if (xs[k] < x ? 1 : 0) + (na(xs[k]) ? 1 : 0) > 0
            k -= size
            k
        else
            k += size
            k
        if size == 0
            if na(xs[k]) ? 1 : 0 + 1 - (x <= xs[k] ? 1 : 0) * (x > xs[k + 1] ? 1 : 0) > 0
                k := na
                k
            break
    k

find_n_periods_ago(i) =>
    fast_search_n(time, time - repeat_period_millis * i)

daysInYear = 365
n_1y = iNumberOfPeriods >= 1 ? find_n_periods_ago(daysInYear * 1) : na
n_2y = iNumberOfPeriods >= 2 ? find_n_periods_ago(daysInYear * 2) : na
n_3y = iNumberOfPeriods >= 3 ? find_n_periods_ago(daysInYear * 3) : na
n_4y = iNumberOfPeriods >= 4 ? find_n_periods_ago(daysInYear * 4) : na
n_5y = iNumberOfPeriods >= 5 ? find_n_periods_ago(daysInYear * 5) : na
n_6y = iNumberOfPeriods >= 6 ? find_n_periods_ago(daysInYear * 6) : na
n_7y = iNumberOfPeriods >= 7 ? find_n_periods_ago(daysInYear * 7) : na
n_8y = iNumberOfPeriods >= 8 ? find_n_periods_ago(daysInYear * 8) : na
n_9y = iNumberOfPeriods >= 9 ? find_n_periods_ago(daysInYear * 9) : na
n_10y = iNumberOfPeriods >= 10 ? find_n_periods_ago(daysInYear * 10) : na
n_11y = iNumberOfPeriods >= 11 ? find_n_periods_ago(daysInYear * 11) : na
n_12y = iNumberOfPeriods >= 12 ? find_n_periods_ago(daysInYear * 12) : na
n_13y = iNumberOfPeriods >= 13 ? find_n_periods_ago(daysInYear * 13) : na
n_14y = iNumberOfPeriods >= 14 ? find_n_periods_ago(daysInYear * 14) : na
n_15y = iNumberOfPeriods >= 15 ? find_n_periods_ago(daysInYear * 15) : na
n_16y = iNumberOfPeriods >= 16 ? find_n_periods_ago(daysInYear * 16) : na
n_17y = iNumberOfPeriods >= 17 ? find_n_periods_ago(daysInYear * 17) : na
n_18y = iNumberOfPeriods >= 18 ? find_n_periods_ago(daysInYear * 18) : na
n_19y = iNumberOfPeriods >= 19 ? find_n_periods_ago(daysInYear * 19) : na
n_20y = iNumberOfPeriods >= 20 ? find_n_periods_ago(daysInYear * 20) : na
n_21y = iNumberOfPeriods >= 21 ? find_n_periods_ago(daysInYear * 21) : na
n_22y = iNumberOfPeriods >= 22 ? find_n_periods_ago(daysInYear * 22) : na
n_23y = iNumberOfPeriods >= 23 ? find_n_periods_ago(daysInYear * 23) : na

daysInMnth = 30
n_1m = iNumberOfPeriods >= 1 ? find_n_periods_ago(daysInMnth * 1) : na
n_2m = iNumberOfPeriods >= 2 ? find_n_periods_ago(daysInMnth * 2) : na
n_3m = iNumberOfPeriods >= 3 ? find_n_periods_ago(daysInMnth * 3) : na
n_4m = iNumberOfPeriods >= 4 ? find_n_periods_ago(daysInMnth * 4) : na
n_5m = iNumberOfPeriods >= 5 ? find_n_periods_ago(daysInMnth * 5) : na
n_6m = iNumberOfPeriods >= 6 ? find_n_periods_ago(daysInMnth * 6) : na
n_7m = iNumberOfPeriods >= 7 ? find_n_periods_ago(daysInMnth * 7) : na
n_8m = iNumberOfPeriods >= 8 ? find_n_periods_ago(daysInMnth * 8) : na
n_9m = iNumberOfPeriods >= 9 ? find_n_periods_ago(daysInMnth * 9) : na
n_10m = iNumberOfPeriods >= 10 ? find_n_periods_ago(daysInMnth * 10) : na
n_11m = iNumberOfPeriods >= 11 ? find_n_periods_ago(daysInMnth * 11) : na
n_12m = iNumberOfPeriods >= 12 ? find_n_periods_ago(daysInMnth * 12) : na
n_13m = iNumberOfPeriods >= 13 ? find_n_periods_ago(daysInMnth * 13) : na
n_14m = iNumberOfPeriods >= 14 ? find_n_periods_ago(daysInMnth * 14) : na
n_15m = iNumberOfPeriods >= 15 ? find_n_periods_ago(daysInMnth * 15) : na
n_16m = iNumberOfPeriods >= 16 ? find_n_periods_ago(daysInMnth * 16) : na
n_17m = iNumberOfPeriods >= 17 ? find_n_periods_ago(daysInMnth * 17) : na
n_18m = iNumberOfPeriods >= 18 ? find_n_periods_ago(daysInMnth * 18) : na
n_19m = iNumberOfPeriods >= 19 ? find_n_periods_ago(daysInMnth * 19) : na
n_20m = iNumberOfPeriods >= 20 ? find_n_periods_ago(daysInMnth * 20) : na
n_21m = iNumberOfPeriods >= 21 ? find_n_periods_ago(daysInMnth * 21) : na
n_22m = iNumberOfPeriods >= 22 ? find_n_periods_ago(daysInMnth * 22) : na
n_23m = iNumberOfPeriods >= 23 ? find_n_periods_ago(daysInMnth * 23) : na

daysInWeek = 7
n_1w = iNumberOfPeriods >= 1 ? find_n_periods_ago(daysInWeek * 1) : na
n_2w = iNumberOfPeriods >= 2 ? find_n_periods_ago(daysInWeek * 2) : na
n_3w = iNumberOfPeriods >= 3 ? find_n_periods_ago(daysInWeek * 3) : na
n_4w = iNumberOfPeriods >= 4 ? find_n_periods_ago(daysInWeek * 4) : na
n_5w = iNumberOfPeriods >= 5 ? find_n_periods_ago(daysInWeek * 5) : na
n_6w = iNumberOfPeriods >= 6 ? find_n_periods_ago(daysInWeek * 6) : na
n_7w = iNumberOfPeriods >= 7 ? find_n_periods_ago(daysInWeek * 7) : na
n_8w = iNumberOfPeriods >= 8 ? find_n_periods_ago(daysInWeek * 8) : na
n_9w = iNumberOfPeriods >= 9 ? find_n_periods_ago(daysInWeek * 9) : na
n_10w = iNumberOfPeriods >= 10 ? find_n_periods_ago(daysInWeek * 10) : na
n_11w = iNumberOfPeriods >= 11 ? find_n_periods_ago(daysInWeek * 11) : na
n_12w = iNumberOfPeriods >= 12 ? find_n_periods_ago(daysInWeek * 12) : na
n_13w = iNumberOfPeriods >= 13 ? find_n_periods_ago(daysInWeek * 13) : na
n_14w = iNumberOfPeriods >= 14 ? find_n_periods_ago(daysInWeek * 14) : na
n_15w = iNumberOfPeriods >= 15 ? find_n_periods_ago(daysInWeek * 15) : na
n_16w = iNumberOfPeriods >= 16 ? find_n_periods_ago(daysInWeek * 16) : na
n_17w = iNumberOfPeriods >= 17 ? find_n_periods_ago(daysInWeek * 17) : na
n_18w = iNumberOfPeriods >= 18 ? find_n_periods_ago(daysInWeek * 18) : na
n_19w = iNumberOfPeriods >= 19 ? find_n_periods_ago(daysInWeek * 19) : na
n_20w = iNumberOfPeriods >= 20 ? find_n_periods_ago(daysInWeek * 20) : na
n_21w = iNumberOfPeriods >= 21 ? find_n_periods_ago(daysInWeek * 21) : na
n_22w = iNumberOfPeriods >= 22 ? find_n_periods_ago(daysInWeek * 22) : na
n_23w = iNumberOfPeriods >= 23 ? find_n_periods_ago(daysInWeek * 23) : na


n_1d = iNumberOfPeriods >= 1 ? find_n_periods_ago(1) : na
n_2d = iNumberOfPeriods >= 2 ? find_n_periods_ago(2) : na
n_3d = iNumberOfPeriods >= 3 ? find_n_periods_ago(3) : na
n_4d = iNumberOfPeriods >= 4 ? find_n_periods_ago(4) : na
n_5d = iNumberOfPeriods >= 5 ? find_n_periods_ago(5) : na
n_6d = iNumberOfPeriods >= 6 ? find_n_periods_ago(6) : na
n_7d = iNumberOfPeriods >= 7 ? find_n_periods_ago(7) : na
n_8d = iNumberOfPeriods >= 8 ? find_n_periods_ago(8) : na
n_9d = iNumberOfPeriods >= 9 ? find_n_periods_ago(9) : na
n_10d = iNumberOfPeriods >= 10 ? find_n_periods_ago(10) : na
n_11d = iNumberOfPeriods >= 11 ? find_n_periods_ago(11) : na
n_12d = iNumberOfPeriods >= 12 ? find_n_periods_ago(12) : na
n_13d = iNumberOfPeriods >= 13 ? find_n_periods_ago(13) : na
n_14d = iNumberOfPeriods >= 14 ? find_n_periods_ago(14) : na
n_15d = iNumberOfPeriods >= 15 ? find_n_periods_ago(15) : na
n_16d = iNumberOfPeriods >= 16 ? find_n_periods_ago(16) : na
n_17d = iNumberOfPeriods >= 17 ? find_n_periods_ago(17) : na
n_18d = iNumberOfPeriods >= 18 ? find_n_periods_ago(18) : na
n_19d = iNumberOfPeriods >= 19 ? find_n_periods_ago(19) : na
n_20d = iNumberOfPeriods >= 20 ? find_n_periods_ago(20) : na
n_21d = iNumberOfPeriods >= 21 ? find_n_periods_ago(21) : na
n_22d = iNumberOfPeriods >= 22 ? find_n_periods_ago(22) : na
n_23d = iNumberOfPeriods >= 23 ? find_n_periods_ago(23) : na


close_last20y = (close + close[n_1y] + close[n_2y] + close[n_3y] + close[n_4y] + close[n_5y] + close[n_6y] + close[n_7y] + close[n_8y] + close[n_9y] + close[n_10y] + close[n_11y] + close[n_12y] + close[n_13y] + close[n_14y] + close[n_15y] + close[n_16y] + close[n_17y] + close[n_18y] + close[n_19y] + close[n_20y] + close[n_21y] + close[n_22y] + close[n_23y] ) / iNumberOfPeriods

close_last24m = (close + close[n_1m] + close[n_2m] + close[n_3m] + close[n_4m] + close[n_5m] + close[n_6m] + close[n_7m] + close[n_8m] + close[n_9m] + close[n_10m] + close[n_11m] + close[n_12m] + close[n_13m] + close[n_14m] + close[n_15m] + close[n_16m] + close[n_17m] + close[n_18m] + close[n_19m] + close[n_20m] + close[n_21m] + close[n_22m] + close[n_23m] ) / iNumberOfPeriods

close_last54w = (close + close[n_1w] + close[n_2w] + close[n_3w] + close[n_4w] + close[n_5w] + close[n_6w] + close[n_7w] + close[n_8w] + close[n_9w] + close[n_10w] 
 + close[n_11w] + close[n_12w] + close[n_13w] + close[n_14w] + close[n_15w] + close[n_16w] + close[n_17w] + close[n_18w] + close[n_19w]
 + close[n_21w] + close[n_22w] + close[n_23w] ) / iNumberOfPeriods
 
close_last54d = (close + close[n_1d] + close[n_2d] + close[n_3d] + close[n_4d] + close[n_5d] + close[n_6d] + close[n_7d] + close[n_8d] + close[n_9d] + close[n_10d] 
 + close[n_11d] + close[n_12d] + close[n_13d] + close[n_14d] + close[n_15d] + close[n_16d] + close[n_17d] + close[n_18d] + close[n_19d]
 + close[n_21d] + close[n_22d] + close[n_23d] ) / iNumberOfPeriods

is_in_last_period_y = time >= timenow - daysInYear * repeat_period_millis
is_in_last_period_m = time >= timenow - daysInMnth * repeat_period_millis
is_in_last_period_w = time >= timenow - daysInWeek * repeat_period_millis
is_in_last_period_d = time >= timenow - repeat_period_millis

plot(iYear and (not iOnlyLastPeriod or is_in_last_period_y) ? close_last20y : na, color=is_in_last_period_y ? color.new(color.black, 0) : color.new(color.black, 50), linewidth=2, title='20 years back')
plot(iMnth and (not iOnlyLastPeriod or is_in_last_period_m) ? close_last24m : na, color=is_in_last_period_m ? color.new(color.green, 0) : color.new(color.green, 50), linewidth=2, title='24 month back')
plot(iWeek and (not iOnlyLastPeriod or is_in_last_period_w) ? close_last54w : na, color=is_in_last_period_w ? color.new(color.blue, 0) : color.new(color.blue, 50), linewidth=2, title='54 weeks back')
plot(iDay and (not iOnlyLastPeriod or is_in_last_period_d) ? close_last54d : na, color=is_in_last_period_d ? color.new(color.red, 0) : color.new(color.red, 50), linewidth=1, title='54 days back')
